// Exercise 7

/**
 * Because the arguments object is not an array, it doesn't have a forEach
 * method. You must either first convert it to an array (Array.from works
 * pretty well) or just capture the arguments into an array with the rest
 * operator (...args).
 */
