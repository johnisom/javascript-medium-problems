// Exercise 6

/**
 * Because the word character class shortcut (\w) includes letters,
 * underscores, *and* numbers, it will return a false positive if a name has
 * numbers or underscores. To fix this, change the name validation regex to
 * /^[a-z]+ [a-z]+$/i
 */
